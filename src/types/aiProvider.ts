/**
 * Prisma enum `AIProvider` — mirror of `prisma/schema.prisma`.
 * Use this at UI/boundary layers when `@prisma/client` enum re-exports confuse the type checker.
 */
export type AIProvider = "OPENAI" | "GROQ" | "ANTHROPIC" | "CUSTOM";
