import { notFound } from "next/navigation";

export const metadata = {
  title: "Import dữ liệu | LumiTask",
};

export default function FinanceImportPage() {
  // Temporarily disabled while improving OCR accuracy.
  notFound();
}
