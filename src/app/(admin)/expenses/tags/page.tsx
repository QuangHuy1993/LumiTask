import { FinanceTagsClient } from "@/features/expenses/ui/FinanceTagsClient";

export const metadata = {
  title: "Thẻ (tag) | LumiTask",
};

export default function FinanceTagsPage() {
  return <FinanceTagsClient />;
}

